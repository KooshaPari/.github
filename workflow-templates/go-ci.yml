name: Go CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: KooshaPari/.github/.github/workflows/ci-go.yml@main
    with:
      go-version: '1.23'
      run-tests: true
      run-lint: true
      run-vet: true
